# GitHub Issue Processor Configuration for TonTon iOS App
# TonTon (トントン) - SwiftUI Health Tracking App

# プロジェクト基本情報
project:
  name: "TonTon (トントン)"
  type: "ios-swift"
  description: "SwiftUI health tracking app with AI-powered meal logging and calorie savings"

# Issue管理リポジトリ（GitHub Issues取得元）
issueRepository:
  owner: "hiroshikodera"  # ← あなたのGitHub Username
  name: "tonton"          # ← このプロジェクトのリポジトリ名

# 修正対象プロジェクトリポジトリ（PR作成先）
targetRepository:
  owner: "hiroshikodera"  # ← あなたのGitHub Username
  name: "tonton"          # ← このプロジェクトのリポジトリ名
  defaultBranch: "master" # メインブランチ名

# ローカルプロジェクト設定
paths:
  root: "/Users/hiroshikodera/repos/_active/apps/tonton"
  source:
    - "Tonton/Tonton/**/*.swift"
    - "Tonton/Tonton.xcodeproj/**/*"
    - "Tonton/Tonton/*.plist"
    - "Tonton/Tonton/*.entitlements"
    - "CLAUDE.md"
    - "README.md"

# ビルド設定（iOS/Swift）
build:
  command: "xcodebuild -project Tonton/Tonton.xcodeproj -scheme Tonton build"
  testCommand: "xcodebuild test -project Tonton/Tonton.xcodeproj -scheme Tonton -destination 'platform=iOS Simulator,name=iPhone 15'"
  lintCommand: "swiftlint --strict"
  
# Issue処理設定
issueProcessing:
  labels:
    - "auto-fix"          # 自動修正対象ラベル
    - "bug"               # バグ修正
    - "enhancement"       # 機能改善
    - "tonton-bug"        # TonTon固有のバグ
  maxConcurrentIssues: 2
  confidenceThreshold: 0.75

# コンテキスト収集設定
context:
  files:
    - "**/*.swift"        # Swiftソースファイル
    - "*.xcodeproj/**/*"  # Xcodeプロジェクトファイル
    - "*.plist"           # 設定ファイル
    - "*.entitlements"    # エンタイトルメントファイル
    - "CLAUDE.md"         # プロジェクト情報
    - "README.md"         # ドキュメント
  maxFiles: 15
  excludePatterns:
    - "*.xcodeproj/xcuserdata/**"
    - "*.xcodeproj/project.xcworkspace/xcuserdata/**"
    - "build/**"
    - "*.dSYM/**"

# TonTon固有の健康アプリ機能
healthAppFeatures:
  healthKit: true
  aiMealLogging: true
  cloudKitSync: true
  calorieSavings: true
  swiftData: true

# 自動修正パターン
autoFixPatterns:
  swiftCompilerErrors: true
  healthKitPermissions: true
  cloudKitIntegration: true
  swiftDataModels: true
  uiAccessibility: true

# Claude Code統合設定
claudeCode:
  useCliTool: true
  maxContextSize: 32000
  thinkingMode: "deep"      # think-hard相当
  validationSteps: true

# GitHub統合設定
github:
  createPullRequests: true
  autoMerge: false          # 手動承認を推奨
  branchNaming: "auto-fix/issue-{number}"
  commitMessageTemplate: |
    🤖 Auto-fix: {title}
    
    Fixes #{number}
    
    Changes made:
    {changes}
    
    🏥 TonTon Health App - Auto-generated by Claude Code
    
    Co-Authored-By: Claude <noreply@anthropic.com>

# 品質チェック
qualityChecks:
  buildSuccess: true
  testExecution: true
  linting: true
  healthKitValidation: true
  accessibilityCheck: true

# 通知設定
notifications:
  slack:
    enabled: false
    webhook: "${SLACK_WEBHOOK_URL}"
  email:
    enabled: false
    recipient: "${NOTIFICATION_EMAIL}"
  console:
    enabled: true
    verbose: true

# ロギング設定
logging:
  level: "info"
  file: "logs/issue-processor.log"
  maxFileSize: "10MB"
  maxFiles: 5
  console: true

# エラー処理設定
errorHandling:
  retryAttempts: 3
  retryDelay: 5000  # 5秒
  timeoutMs: 300000 # 5分
  gracefulShutdown: true

# パフォーマンス設定
performance:
  maxMemoryUsage: "1GB"
  concurrentOperations: 2
  cacheEnabled: true
  cacheTTL: 3600 # 1時間